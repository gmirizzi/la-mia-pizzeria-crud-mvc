@{
	ViewData["Title"] = "Categorie";
}

@model List<Category>

<div class="container">
	<h1>Categorie</h1>
	<table class="table table-striped table-dark">
		<thead>
			<tr>
				<th scope="col">#</th>
				<th scope="col">Category</th>
				<th scope="col">Handle</th>
			</tr>
		</thead>
		<tbody>
			@foreach (Category category in Model)
			{
				<tr>
					<th scope="row">@category.CategoryId</th>
					<td>@category.Name</td>
					<td>
						<a class="btn btn-primary me-2" asp-controller="Categories" asp-action="Edit" asp-route-id="@category.CategoryId">Edit</a>
						<a class="btn btn-danger" asp-controller="Categories" asp-action="Delete">Delete</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
	<div class="mt-3">
		<a class="btn btn-primary" asp-controller="Categories" asp-action="Create">Add</a>
	</div>
</div>
